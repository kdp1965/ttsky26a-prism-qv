title: "TinyQV LUT2"
version: "1.0"
fracturable: false

muxes: { size: 4, count: 3 }
decision-tree: {
	static-components: [ # ... should match STEW cfg order
		{ type:"lut", size:2, offset:0 },
	],
	conditional-components: [ # ... should match STEW cfg order
		{ type:"lut", size:1, offset: 2 },
	]
}
stew: {
	count: 16,
	size: 45,
	items: [
		{ type:"inc", offset:  0, size: 1 },
		{ type:"mux", offset:  1, size:12 },
		{ type:"jmp", offset: 13, size: 4 }, # The JumpTo state
		{ type:"out", offset: 28, size:11 }, # Default outputs
		{ type:"out", offset: 17, size:11 }, # JumpTo outputs
		{ type:"cfg", offset: 39, size: 4 }, # JumpTo LUT bits
		{ type:"cfg", offset: 43, size: 2 }, # Condout LUT bits
	]
}
